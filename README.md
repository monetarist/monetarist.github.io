# monetarist.github.io

Commands:

Paste into RStudio
```bash
rmarkdown::render_site(encoding = "UTF-8")
```

Paste into Terminal
```bash
git add .
git commit -am "Any Comment Here"
git push origin master
```
